import { evilPandaStrategy } from './evil-panda'

/**
 * Evil Panda is the only active strategy.
 * Direct export — no registry, no lookup, no multi-strategy indirection.
 */
export { evilPandaStrategy }
export { evilPandaStrategy as default }
